class QuantityModel {
  final int id;
  final int user_id;
  final int product_id;
  final int quantity;
  // final String follow_status_user;

  QuantityModel(this.id, this.user_id, this.product_id, this.quantity);
}
